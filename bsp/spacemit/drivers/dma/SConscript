# for module compiling
import os
from building import *

cwd  = GetCurrentDir()
src     = Glob('*.c')
CPPPATH = [cwd]

group = DefineGroup('Drivers_dma', src, depend = ['BSP_USING_DMA'], CPPPATH = CPPPATH)

Return('group')
